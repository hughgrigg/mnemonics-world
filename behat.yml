#behat.yml
default:
    autoload:
        '': %paths.base%/app/tests/acceptance/bootstrap
    suites:
        default:
            paths:
                - %paths.base%/app/tests/acceptance
    extensions:
        Behat\MinkExtension:
            goutte: ~
            base_url: http://www.mnemonicsworld.dev:8000